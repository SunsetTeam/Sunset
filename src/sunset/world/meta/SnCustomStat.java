package sunset.world.meta;

import arc.scene.ui.layout.Table;

/** Интерфейс для контента, который отображает свои параметры самостоятельно. */
public interface SnCustomStat {
    void displayStats(Table table);
}
